spring:
  datasource:
    url: jdbc:mysql://agenda-db:3306/agenda_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
